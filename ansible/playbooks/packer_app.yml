- name: Install App
  hosts: all
  become: true
  tasks:
    - name: Install ruby
      apt:
        name: "{{ packages }}"
        update_cache: yes
      vars:
        packages:
          - ruby-full
          - ruby-bundler
          - build-essential
